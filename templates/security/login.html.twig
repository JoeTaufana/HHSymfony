{% extends 'base.html.twig' %}
{# {% block head %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('css/creation.css') }}">
{% endblock %} #}
{% block title %}Login
{% endblock %}

{% block body %}

<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('logout') }}">Logout</a>
        </div>
    {% endif %}

		
				<form class="formulaire" method="post">
					<div id="display_start">
						<label for="mail">Email :</label><br>
						<input type="email" id="mail2" placeholder="Entrez un email*" name="email"><br>
						<label for="password">Mot de passe :</label>
						<input type="password" id="mdp2" minlength="4" maxlength="20" placeholder="Mot de passe*" name="password">
					</div>
					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate')}}">
					<input class="btn ocre valider" type="submit" value="Valider">
				</form>
			</section>
		</main>

{% endblock %}
