{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(Clients) }}
            {{ form_row(Clients.prenom) }}
            {{ form_row(Clients.nom) }}
            {{ form_row(Clients.telephone) }}
            {{ form_row(Clients.optInSms) }}
            {{ form_row(Clients.optInMail) }}
            {{ form_row(Clients.dateNaissance) }}
            {{ form_row(Clients.nombreEnfant) }}

            <div id="bouton">
				<button type="submit" class="btn ocre">Valider</button>
			</div>
            
    {{ form_end(Clients) }}
{% endblock %}
